name: project-x

on:
  workflow_run:
    workflows: [CI]
    types:
      - completed

Jobs:

  Test:
    name: Test
    runs-on: [self-hosted]
    steps:
      - name: Get version number
        shell: bash
        run: env