apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: memory-requests
spec:
  validationFailureAction: audit
  background: true
  rules:
  - name: memory-requests
    match:
      any:
      - resources:
          kinds:
          - Pod
    validate:
      message: "Memory resource requests are required."
      pattern:
        spec:
          containers:
          - resources:
              requests:
                memory: "?*"