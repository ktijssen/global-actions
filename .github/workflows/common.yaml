name: common

on:
  workflow_call:
    secrets: inherit
    
jobs:
  SetVersion:
    name: Semantic Versioning
    runs-on: [self-hosted]
    steps:
      - name: Get version number
        shell: bash
        run: |
          env
          ./global-actions/common.sh