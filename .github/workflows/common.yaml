name: common

on:
  workflow_call:
    
jobs:
  SetVersion:
    name: Semantic Versioning
    runs-on: [self-hosted]
    steps:
      - name: Get version number
        shell: bash
        run: ls -la 