name: common

on:
  workflow_call:

jobs:
  BuildManifests:
    name: Build Manifests files
    needs: SetVersion
    runs-on: [self-hosted]
    steps:
      - name: Build Manifest
        shell: bash
        run: ./global-actions/build/build_manifests.sh