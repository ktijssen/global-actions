name: Manifests

on:
  workflow_call:
    inputs:
      projectName:
        default: ${{ github.workflow }}
        required: false
        type: string
      containerRegistry:
        default: ''
        required: false
        type: string
      gitTag:
        required: true
        type: string

jobs:
  BuildManifests:
    name: Build Manifests files
    runs-on: [self-hosted]
    steps:
      - name: Checkout Project Repo
        uses: actions/checkout@v3
      # - name: Check out global-actions repository
      #   uses: actions/checkout@v3
      #   with:
      #     repository: KTijssen/global-actions
      #     ref: beta
      #     # token: ${{ secrets.PAT_TOKEN }}
      #     path: "global-actions"

      - name: Build Manifest
        shell: bash
        run: |
          echo ${{ inputs.gitTag }}
          ls -la
          echo "Run Build Manifests here!"
          #./global-actions/build/manifests/build_manifests.sh